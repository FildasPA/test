    # Affiche la durée totale du nouveau contenu
    # command = 'ffprobe -i {file} -show_entries format=duration -v quiet -of csv="p=0"'.\
    #     format(file=FINAL_FILE_DESTINATION)

    # # print FINAL_FILE_DEST
    # # print command
    # print "New file duration: "
    # try:
    #     subprocess.check_call(command.split())
    # except subprocess.CalledProcessError as exp:
    #     print('Exception caught: {}'.format(exp))
